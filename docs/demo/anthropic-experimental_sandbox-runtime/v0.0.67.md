# Release-note fact check: anthropic-experimental/sandbox-runtime v0.0.66 → v0.0.67

Judge engine: `claude-cli/haiku` · 23 commits, 148 files, +7237/−555

**Trust score: 85/100 (solid)** — correctness 100 · completeness 50 · risk 90

## Risk flags

- **warn** Undocumented changes in dependencies paths (package-lock.json, package.json)

## Summary

| Verdict | Count |
|---|---|
| verified | 8 |
| partial | 0 |
| no-evidence | 0 |
| contradicted | 0 |
| skipped | 0 |

## Claims

### Windows

- **verified** (0.90) — `git.safeDirectories` config — `safe.directory` without write grants; fixes dubious-ownership on subdirectory launches (also emits on macOS/Linux) (#403)
  - evidence: pr-anchor+lexical · aa9f6f30 · src/sandbox/linux-sandbox-utils.ts, src/sandbox/macos-sandbox-utils.ts, src/sandbox/sandbox-config.ts +4 more · fns: LinuxSandboxParams, wrapCommandWithSandboxLinux, MacOSSandboxParams, wrapCommandWithSandboxMacOS
  - PR #403 is in the release range (aa9f6f3051); identifiers safe.directory, git.safeDirectories, macOS appear in its diff.
- **verified** (0.95) — Deny targets that don't exist yet are materialized as placeholders and stamped, closing the create-past-a-deny hole; trailing `/` marks directory targets (#404)
  - evidence: pr-anchor+lexical+llm · 1172a926 · src/sandbox/sandbox-manager.ts, src/sandbox/windows-sandbox-utils.ts, test/sandbox/glob-expand.test.ts +5 more · fns: computeWindowsFsAccessSet, wrapWithSandboxArgv, uninstallWindowsSandbox, describe
  - New `create_placeholder_chain()` function in path_id.rs materializes missing intermediates and empty leaf for non-existent deny targets. In cli.rs, `canonicalize_ace_targets()` calls this when encountering `CanonError::NotFound` on deny targets, stamping leaf with full `SbAce::Deny` and intermediates with `SbAce::DenyDelete`. Trailing-separator handling: `leaf_is_dir = p.ends_with(['\\', '/'])` in cli.rs; smoke test A31(d) confirms trailing `\` materializes as directory. All changes integrated w
- **verified** (0.95) — Kill-chain hardening: broker error paths always reap the runner; CI smoke rows pin the no-survivors invariant (#405)
  - evidence: pr-anchor+llm · 5d9821c3 · vendor/srt-win-src/ci/smoke-kill.ps1, vendor/srt-win-src/src/logon.rs, .github/workflows/integration-tests.yml · fns: param, spawn_runner, wstr
  - The new smoke-kill.ps1 script implements K1/K2 invariant tests asserting 'NO sandbox process survives the broker' and is wired into CI via integration-tests.yml. The logon.rs changes show the SpawnedChild guard is now never defused, with its Drop implementation calling TerminateProcess 'on every in-process exit', ensuring the runner is always reaped regardless of error paths or graceful exits.
- **verified** (0.90) — Persistent MITM CA: `ca.json` under the protected state dir, atomic writes, `notBefore`/`notAfter` validation, auto-load at `initialize()`, `generateCa`/`validateCaPair` exports (#406)
  - evidence: pr-anchor+lexical · 29701f19 · src/index.ts, src/sandbox/mitm-ca.ts, src/sandbox/sandbox-manager.ts +3 more · fns: rsaSha256SignNative, loadCA, generateEphemeralCA, z.object
  - PR #406 is in the release range (29701f19e5); identifiers generateCa, validateCaPair, notBefore, notAfter appear in its diff.
- **verified** (0.90) — **Breaking:** `windows.srtWin.path` is required — no implicit vendored-exe fallback; pass the exported `VENDORED_SRT_WIN_EXE` to use the packaged binary. Fact-only argv-too-long message (#413)
  - evidence: pr-anchor+lexical · 86c32945 · src/cli.ts, src/index.ts, src/sandbox/sandbox-config.ts +2 more · fns: main, z.object, WindowsSandboxParams, repoRoot
  - PR #413 is in the release range (86c3294541); identifiers VENDORED_SRT_WIN_EXE, windows.srtWin.path appear in its diff.
- **verified** (0.90) — Typed `WindowsSandboxError` codes with `.subcommand`, single-spawn `srt-win status --json` + `checkWindowsSandboxStatus()` (#414)
  - evidence: pr-anchor+lexical · c3eb609c · src/index.ts, src/sandbox/sandbox-manager.ts, src/sandbox/windows-sandbox-utils.ts +1 more · fns: initialize, parseWindowsBinShell, getSrtWinPath, runSrtWin
  - PR #414 is in the release range (c3eb609ca0); identifiers WindowsSandboxError, checkWindowsSandboxStatus, .subcommand appear in its diff.
- **verified** (0.95) — Typed `mapped_drive_cwd` error (exit 16) for network-drive working directories; the broker never stats UNC literals (#415)
  - evidence: pr-anchor+lexical+llm · 482d46dd · src/sandbox/windows-sandbox-utils.ts, test/sandbox/glob-expand.test.ts, vendor/srt-win-src/src/cli.rs · fns: stripExtendedPathPrefix, normalizePathForSandbox, uninstallWindowsSandbox, expandWindowsFsPaths
  - The diff shows the typed error `MappedDriveCwd` struct in logon.rs for network-drive working directories, explicit exit code 16 in cli.rs (`std::process::exit(16)`), and a parser `parseWindowsSandboxError` in windows-sandbox-utils.ts that surfaces the error. The `isUncPath` function in sandbox-utils.ts with documentation stating 'The broker uses this to skip `stat`/`realpath` on UNC **literals**' and the test case 'passes UNC literals through without stat (broker never touches SMB)' directly con
- **verified** (0.90) — Async dependency/status/install APIs (`checkDependenciesAsync` — additive, sync API unchanged); install/uninstall timeout default 120s; `windowsHide` on sync spawns (#395)
  - evidence: pr-anchor+lexical · 03048279 · src/sandbox/sandbox-manager.ts, src/sandbox/windows-sandbox-utils.ts, test/sandbox/check-dependencies.test.ts +1 more · fns: initialize, isSandboxingEnabled, checkDependencies, ISandboxManager
  - PR #395 is in the release range (030482795e); identifiers checkDependenciesAsync, windowsHide appear in its diff.

## Undocumented changes

- `ea824ecd` Merge main (through v0.0.66, incl. #399 body substitution) into sigv4 branch (+1251/−29, 15 files)
- `7bff0524` Add SigV4 signer, classifier, and official test-suite vectors (+1182/−0, 116 files)
- `51b0e001` Add AWS credential pair registry and SigV4 re-sign planner (+1028/−0, 2 files)
- `b3f458ae` Add end-to-end SigV4 re-sign integration and policy tests (+544/−0, 1 files)
- `0da2dc3d` Cap the body buffer for SigV4 literal-hash re-signing at 64 MiB (+158/−6, 4 files)
- `dcbe5ab5` Wire SigV4 re-signing into the TLS-terminating proxy (+92/−3, 3 files)
- `eb1d419b` Close the presigned-policy bypass via a non-AWS Authorization header (+76/−13, 3 files)
- `9ff64262` Fix Bun stream corruption in the TLS-terminate relay (pull-mode reads) (+78/−1, 2 files)
- `31156a27` Fail closed when a signed header is stripped before re-signing (+51/−5, 2 files)
- `21d8f75e` Release v0.0.67 (#422) (+3/−3, 2 files)
- `e1bea57f` Mark vendored SigV4 test vectors linguist-generated (+5/−0, 1 files)
